Running `nx build styleguide` will work perfectly fine.

Running `nx build-custom styleguide` will result in the error that the files 
which are imported from the component-project are not under the `rootDir`.

